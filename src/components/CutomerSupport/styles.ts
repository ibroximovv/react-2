import styled from "styled-components";

export const CustomerSupportWrapper = styled.li`
    display: flex;
    gap: 30px;
    /* width: 365px; */
    align-items: center;
    p {
        color: var(--color-text);
        font-weight: 400;
        font-size: 16px;
        line-height: 26px;
        letter-spacing: 0px;
    }
`